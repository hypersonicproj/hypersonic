package filesystem

import (
	"hypersonic/internal/domain"
	"hypersonic/internal/usecase/search"
)

func (s *filesystem) FindArtistsNameAsc(option search.FindOption) ([]domain.Artist, error) {
	panic("unimplemented")
}
